Convert tiles from tms or mod_tile cache to SAS.Planet sqlite cache.

Usage:

  ./tms2sqlite.py \[-f format] \[-z zoom] input_dir output_dir

Formats: tms, htms (hashed tms, old mod_tile), meta (hashed metatiles, modern mod_tile)
